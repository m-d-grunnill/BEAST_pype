[build-system]
requires = ["setuptools >= 77.0.3", "unidep", "wheel >= 0.38" ]
build-backend = "setuptools.build_meta"

[project]
name = "beast_pype"
version = "0.8.0"
dynamic = ["dependencies"]
authors = [
    {name="Martin Grunnill", email="Martin.Grunnill@phac-aspc.gc.ca"},
    {name="Carmen Lia Murall", email="Carmen.Lia.Murall@phac-aspc.gc.ca"}
]
maintainers = [
    {name="Martin Grunnill", email="Martin.Grunnill@phac-aspc.gc.ca"},
    {name="Carmen Lia Murall", email="Carmen.Lia.Murall@phac-aspc.gc.ca"}
]
description = "A package of BEAST 2 pipelines for phylodynamics."
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering"
]
# keywords should contain search terms so users can
# find our project
keywords = [
    "Phylodynamics",
    "Epidemiology",
    "Ecology",
    "Virology",
    'Genomics'
]


[project.scripts]
beast_pype = "beast_pype.cli:beast_pype"

### Adding non python files
[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["*.ipynb"]


### Package dependency setup via setuptools and unidep
[tool.hatch.metadata.hooks.unidep]
# Enable the unidep plugin

[tool.hatch.metadata]
allow-direct-references = true

[tool.unidep]
channels = ["conda-forge", 'bioconda']
dependencies = [
    # Bioinformatics
    { conda = 'beast2 >= 2.6.3'},
    { pip = 'beast2-xml >= 1.6.2'},
    { pip = 'dark-matter'},
    { pip = 'biopython'},
    { conda = 'iqtree'},
    { pip = 'phylo-treetime'},
    { conda = 'seqkit'},
    { pip = 'ete3'},
  # Python Data Sci
    { pip = 'scipy'},
    { pip = 'numpy'},
    { pip = 'pandas'},
 # Plotting figures.
    { pip = 'seaborn'},
    { pip = 'matplotlib >= 3.10'},
# MCMC diagnostics_of_selection
    { pip = 'arviz >= 1.2.0'},
    { pip = 'xarray'},
# Utils
    { pip = 'click'},
    { pip = 'pyyaml'},
    { conda = 'parallel'},
    { pip = 'python-dateutil'},
# Jupyter & Jutyper based workflow
    { pip = 'jupyter'},
    { pip = 'papermill'},
    { pip = 'nbconvert'},
    { pip = 'bash_kernel'},
    { pip = 'ipykernel'},
    { pip = 'nbformat'},
    { pip = 'ipywidgets'},
    { conda = 'r-irkernel'},
# R components
    { conda = 'bioconductor-treeio'},
    { conda = 'bioconductor-ggtree'},
    { conda = 'r-ggplot2'},
    { conda = 'r-readr'},
    { conda = 'r-lubridate'},
    { conda = 'r-dplyr'},
    { conda = 'r-png'},
    { conda = 'r-tidyr'},
    { conda = 'r-htmltools'}
]

[project.urls]
Homepage = "https://github.com/m-d-grunnill/BEAST_pype"
Repository = "https://github.com/m-d-grunnill/BEAST_pype"
Issues = "https://github.com/m-d-grunnill/BEAST_pype/issues"
Documentation = "https://github.com/m-d-grunnill/BEAST_pype/wiki"