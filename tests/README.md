# Unit Tests

Unit testing is done using [PyTest](https://docs.pytest.org/en/stable/). To run all unit
tests use the following command from the root of the repository:
```bash
conda activate beast_pype
pytest test/
```