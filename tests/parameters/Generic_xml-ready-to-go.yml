number_of_beast_runs: 1 #Number of chains to use (repeated runs to do) when running BEAST.
seed_to_rule_them_all: 42 #    If provided, this seed is used for all BEAST runs. Otherwise, a random seed is generated for each run.
overall_save_dir: Local_Test-Generic_xml-ready-to-go #    Path to where you are saving all the runs of this workflow.
ready_to_go_xml:  tests/ready_to_go_xmls/Netherlands_2024_measles_D8_Coal_Exp.xml #    Path to a BEAST 2 xml that you wish to run unaltered. If provided phases 2i, 2ii and 3 are skipped.
